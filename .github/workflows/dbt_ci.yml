name: dbt CI

on:
  pull_request:
    branches: [main]
    paths:
      - 'dbt_project/**'
      - '.github/workflows/dbt_ci.yml'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install sqlfluff
        run: pip install sqlfluff==3.* sqlfluff-templater-dbt==3.*
      - name: Lint dbt models
        working-directory: dbt_project
        run: sqlfluff lint models --dialect snowflake

  dbt_build:
    runs-on: ubuntu-latest
    needs: lint
    env:
      SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
      SNOWFLAKE_CI_USER: ${{ secrets.SNOWFLAKE_CI_USER }}
      SNOWFLAKE_CI_PRIVATE_KEY: ${{ secrets.SNOWFLAKE_CI_PRIVATE_KEY }}
      DBT_PROFILES_DIR: ./dbt_project
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dbt
        run: pip install dbt-snowflake==1.8.*
      - name: Use CI profiles.yml
        run: cp dbt_project/profiles_example.yml dbt_project/profiles.yml
      - name: dbt deps
        working-directory: dbt_project
        run: dbt deps
      - name: dbt seed
        working-directory: dbt_project
        run: dbt seed --target ci
      - name: dbt snapshot
        working-directory: dbt_project
        run: dbt snapshot --target ci
      - name: dbt build (models + tests)
        working-directory: dbt_project
        run: dbt build --target ci
